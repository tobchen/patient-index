logging:
  level:
    org.jooq.tools.LoggerListener: debug
    de.tobchen: trace

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: password
